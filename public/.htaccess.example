# Rename to .htaccess on Apache if needed.
<IfModule mod_rewrite.c>
    RewriteEngine On
    # Optional: force HTTPS
    # RewriteCond %{HTTPS} !=on
    # RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>
